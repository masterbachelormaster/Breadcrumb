name: Breadcrumb
options:
  bundleIdPrefix: com.roger
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "16.0"
settings:
  SWIFT_VERSION: "6.0"
targets:
  Breadcrumb:
    type: application
    platform: macOS
    sources: [Breadcrumb]
    info:
      path: Breadcrumb/Info.plist
      properties:
        LSUIElement: true
        LSMinimumSystemVersion: $(MACOSX_DEPLOYMENT_TARGET)
        CFBundleDisplayName: Breadcrumb
        CFBundleIconFile: ""
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSMicrophoneUsageDescription: "Breadcrumb uses the microphone for speech-to-text in status updates."
        NSSpeechRecognitionUsageDescription: "Breadcrumb uses speech recognition to transcribe your status updates."
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.roger.breadcrumb
      MARKETING_VERSION: "1.4.3"
      CURRENT_PROJECT_VERSION: "34"
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      # Weak-link FoundationModels so the app still launches on macOS versions
      # where an AI symbol is missing — the AI feature then disables itself at
      # runtime instead of the app dying silently before launch.
      OTHER_LDFLAGS: ["-weak_framework", "FoundationModels"]
  BreadcrumbTests:
    type: bundle.unit-test
    platform: macOS
    sources: [BreadcrumbTests]
    dependencies:
      - target: Breadcrumb
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.roger.breadcrumb-tests
      GENERATE_INFOPLIST_FILE: YES
